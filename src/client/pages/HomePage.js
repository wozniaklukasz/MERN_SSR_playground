import React from "react";

const HomePage = () => {
  return (
    <div>
      <div>HOME 2</div>
      <button onClick={() => console.log("click")}>click</button>
    </div>
  );
};

export default HomePage;
